// Protocols are like Swift protocols or Kotlin interfaces
protocol Vehicle {
    // Default implementation are forbidden in protocols
    fn drive()
}

// Extending a prototype
class Car: Vehicle {
    // Override keyword is *not* needed
    fn doNoise() {
        print("Fast")
    }
}