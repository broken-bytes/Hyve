module App

struct World {
    var index: Int

    fn hello() -> Int {
        return index
    }

    mut fn setIndex(index: Int) {
        self.index = index
    }
}

fn main() -> Int {
    var world = World { index: 0 }

    return world.hello()
}
